# Optional. Without these the build uses src/data/colleges.json, which is
# committed and complete. Set them to let the admin panel's site_colleges table
# feed the build. Read at build time only — see docs/DEPLOYMENT.md.
SUPABASE_URL=
SUPABASE_ANON_KEY=
