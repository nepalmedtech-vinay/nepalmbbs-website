---
import { SITE } from '../lib/routes.js';
import Navbar from '../components/Navbar.astro';
import MobileMenu from '../components/MobileMenu.astro';
import Ticker from '../components/Ticker.astro';
import ContactBar from '../components/ContactBar.astro';
import Footer from '../components/Footer.astro';
import WhatsAppFloat from '../components/WhatsAppFloat.astro';
import ChatWidget from '../components/ChatWidget.astro';
import AdminPanel from '../components/AdminPanel.astro';

const {
  title,
  description,
  path,
  isHome = false,
  bare = false,          // pages that supply their own chrome
  ogImage = 'https://nepalmbbs.in/assets/brand/icon-512.png',
  schema = null,
} = Astro.props;

const canonical = new URL(path, SITE).href;
---
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">

<title>{title}</title>
<meta name="description" content={description}>
<link rel="canonical" href={canonical}>

<meta property="og:type" content={isHome ? 'website' : 'article'}>
<meta property="og:site_name" content="NepalMBBS.in">
<meta property="og:locale" content="en_IN">
<meta property="og:title" content={title}>
<meta property="og:description" content={description}>
<meta property="og:url" content={canonical}>
<meta property="og:image" content={ogImage}>
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content={title}>
<meta name="twitter:description" content={description}>
<meta name="twitter:image" content={ogImage}>

<meta name="theme-color" content="#F4F6FB">
<link rel="manifest" href="/manifest.json">
<link rel="icon" href="/assets/brand/icon-192.png" sizes="192x192" type="image/png">
<link rel="apple-touch-icon" href="/assets/brand/apple-touch-icon.png">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400..900&family=Geist:wght@300..800&display=swap" rel="stylesheet">

<!-- Order is load-bearing, and bridge.css goes LAST on purpose.
     The obvious arrangement — aliases first, legacy after — does not work: the
     legacy sheets carry their own !important on the very declarations that have
     to be overridden (.tab-card sets an important white in two files), and
     between two !important rules of equal specificity the later sheet wins.
     Loading bridge last settles that without escalating specificity anywhere.
     Its :root aliases are unaffected by the move, because var() resolves at
     computed-value time rather than at parse time. -->
{/* Ten separate sheets, deliberately. Bundling them into one 169 kB file
    was tried and measured WORSE — LCP 1632ms → 1916ms — because nothing can
    apply until a single file finishes, while ten stream in parallel. The
    round-trip argument for bundling assumes HTTP/1.1 head-of-line blocking;
    Netlify serves HTTP/2, where the requests multiplex and the only thing
    left is the serialisation penalty. tools/bundle-css.mjs is kept for the
    record; it is not wired in. */}
<link rel="stylesheet" href="/assets/theme/engine.css">
<link rel="stylesheet" href="/assets/css/base.css">
<link rel="stylesheet" href="/assets/css/components.css">
<link rel="stylesheet" href="/assets/css/sections.css">
<link rel="stylesheet" href="/assets/css/trust.css">
<link rel="stylesheet" href="/assets/theme/bridge.css">
<link rel="stylesheet" href="/assets/theme/glass.css">
<link rel="stylesheet" href="/assets/theme/motion.css">
<link rel="stylesheet" href="/assets/theme/panel.css">
{/* Last two, in this order: chrome.css finishes the dark→light migration for
    the site chrome (ticker, contact bar) that bridge.css only half-completed;
    interiors.css does the same for what is *inside* the routes, which that
    migration never reached. See the header comment in each. */}
<link rel="stylesheet" href="/assets/theme/chrome.css">
<link rel="stylesheet" href="/assets/theme/interiors.css">
{/* 36 kB of base64 PNG for a decorative noise overlay, and it was
    render-blocking: nothing could paint until it arrived. media="print"
    keeps the browser from blocking on it; runtime.js flips it to "all"
    once the page is up. Flipping it from a script rather than an inline
    onload handler is deliberate -- the CSP allows no inline handlers. */}
<link rel="stylesheet" href="/assets/gfx/grain.css" media="print" data-late-style>

{schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}

<!-- Paint the saved theme before first paint, or a returning visitor sees the
     default palette for a frame. try/catch because a private window throws on
     localStorage, and a theme is never worth a blank page. -->
<script is:inline>
  (function () {
    try {
      var t = JSON.parse(localStorage.getItem('nmb_theme_v1') || 'null');
      if (!t) return;
      var s = document.documentElement.style;
      var map = {
        brand: '--brand', brand2: '--brand-2', ink: '--g-ink', base: '--g-base',
        au1: '--au-1', au2: '--au-2', au3: '--au-3', au4: '--au-4',
        auOpacity: '--au-opacity', auScale: '--au-scale',
        mOpacity: '--m-opacity', mBorder: '--m-border', mInner: '--m-inner',
        radius: '--sh-radius', depth: '--dp', tyScale: '--ty-scale', sp: '--sp',
        mo: '--mo', fxGrain: '--fx-grain', tyWeight: '--ty-weight', tyWeightD: '--ty-weight-d'
      };
      for (var k in map) if (t[k] !== undefined) s.setProperty(map[k], t[k]);
      if (t.mBlur !== undefined)    s.setProperty('--m-blur', t.mBlur + 'px');
      if (t.mSaturate !== undefined) s.setProperty('--m-saturate', t.mSaturate + '%');
      if (t.auBlur !== undefined)   s.setProperty('--au-blur', t.auBlur + 'px');
      if (t.auSpeed !== undefined)  s.setProperty('--au-speed', t.auSpeed + 's');
      if (t.border !== undefined)   s.setProperty('--sh-border', t.border + 'px');
    } catch (e) {}
  })();
</script>
<style is:inline>:root{--grain-src:var(--pm-grain-src)}</style>
</head>
<body>

<a href="#main" class="skip-to-main">Skip to content</a>

<div class="au" aria-hidden="true"><i></i><i></i><i></i><i></i></div>
<div class="grain" aria-hidden="true"></div>
<div class="m-progress-rail" aria-hidden="true"><i class="m-progress"></i></div>

{!bare && <><Navbar /><MobileMenu /></>}

{/* The skip link needs a landmark to land on, and a page with a nav on every
    route needs a main region regardless. Bare pages (the console, the portal)
    bring their own <main id="main">, so wrapping here would nest two. */}
{bare ? <slot /> : <main id="main"><slot /></main>}

{!bare && <><Ticker /><ContactBar /><Footer /><WhatsAppFloat /><ChatWidget /><AdminPanel /></>}

<!-- Theme panel. Lives behind the admin gate on real pages; the standalone
     preview mounts its own toggle. -->
<aside id="theme-panel" class="tp-sheet" hidden aria-label="Appearance settings"></aside>

{/* defer, not async: these files depend on each other in source order
    (config.js defines SB/AK before leads.js reads them) and async would
    race them. Deferred scripts still run before DOMContentLoaded, so
    every listener they attach is attached in time. */}
<script is:inline defer src="/assets/js/config.js"></script>
<script is:inline defer src="/assets/js/auth.js"></script>
<script is:inline defer src="/assets/theme/engine.js"></script>
<script is:inline defer src="/assets/theme/panel.js"></script>
<script is:inline defer src="/assets/theme/aurora-gl.js"></script>

{!bare && (
  <>
    <script is:inline defer src="/assets/js/boot.js"></script>
    <script is:inline defer src="/assets/js/i18n.js"></script>
    <script is:inline defer src="/assets/js/navigation.js"></script>
    <script is:inline defer src="/assets/js/leads.js"></script>
    <script is:inline defer src="/assets/js/reveal.js"></script>
    <script is:inline defer src="/assets/js/chatbot.js"></script>
    <script is:inline defer src="/assets/js/colleges.js"></script>
    <script is:inline defer src="/assets/js/admin.js"></script>
    <script is:inline defer src="/assets/js/effects.js"></script>
    <script is:inline defer src="/assets/js/actions.js"></script>
    <script is:inline defer src="/assets/js/chrome.js"></script>
  </>
)}

<script is:inline defer src="/assets/theme/runtime.js"></script>
<script is:inline defer src="/assets/theme/motion.js"></script>
</body>
</html>
